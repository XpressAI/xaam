// This file runs on the server before rendering the layout
// We can use it to load data that will be available to all pages

export function load() {
  return {
    // You can add server-loaded data here if needed
  };
}