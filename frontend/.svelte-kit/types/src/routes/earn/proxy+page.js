// @ts-nocheck
// This is a minimal route loader for the earn page
// It can be expanded in the future to load agent data or other necessary information

/** @param {Parameters<import('./$types').PageLoad>[0]} event */
export async function load({ fetch, params }) {
  return {
    // Return any data needed for the page here
  };
}